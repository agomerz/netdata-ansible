---

- name: Create node claim file
  ansible.builtin.template:
    src: claim.conf.j2
    dest: "{{ netdata_config_dir }}/claim.conf"
    owner: root
    group: root
    mode: '0644'
  when: netdata_claim
  notify: Restart Netdata
