---

- name: Create node claim file
  ansible.builtin.template:
    src: claim.conf.j2
    dest: "{{ netdata_config_dir }}/claim.conf"
    owner: root
    group: root
    mode: '0644'
    when: node_claim_token | length > 0
  notify: Restart Netdata
